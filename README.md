# 🤖 VK Bot - Добрая душа.ru

> Платформа для создания и просмотра благотворительных инициатив в экосистеме ВКонтакте Max

## 📋 Содержание

- [Описание](#-описание)
- [Архитектура](#️-архитектура)
- [Технологический стек](#-технологический-стек)
- [Требования](#-требования)
- [Установка и запуск](#-установка-и-запуск)
  - [Локальный запуск](#локальный-запуск)
  - [Запуск через Docker](#запуск-через-docker)

---

## 🎯 Описание

**Добрая душа.ru** — это бот для платформы ВКонтакте Max, который позволяет пользователям:

- 📱 Просматривать карточки благотворительных инициатив
- ✨ Создавать собственные инициативы
- 👀 Отслеживать просмотренные карточки
- 🏷️ Фильтровать инициативы по категориям (благотворительность, эко-мероприятия, волонтерство)
- 📊 Просматривать статистику своих карточек

Проект состоит из трех основных компонентов:
- **VK Bot** — бот для взаимодействия с пользователями
- **REST API** — серверная часть для обработки запросов
- **Web Frontend** — веб-интерфейс на React для отображения в Max

---

## 🏗️ Архитектура

Проект построен по модульной архитектуре с разделением на следующие компоненты:

### Компоненты системы

#### 1. **VK Bot** (`bot/`)
- Обрабатывает события от платформы Max
- Отправляет мотивационные сообщения пользователям
- Использует библиотеку `@maxhub/max-bot-api`

#### 2. **REST API** (`api/`)
- Fastify сервер с поддержкой CORS и multipart
- Обрабатывает CRUD операции с карточками
- Отслеживает просмотры и статистику
- Порт по умолчанию: `8788`

#### 3. **Web Frontend** (`max-web/`)
- React приложение на Vite
- Использует компоненты `@maxhub/max-ui`
- Коммуницирует с API через HTTP запросы
- Порт по умолчанию: `4173` (preview mode)

#### 4. **Database Layer** (`db/`)
- Утилиты для работы с MongoDB
- Операции с карточками, просмотрами и пользователями
- Централизованное подключение к БД

---

## 🛠 Технологический стек

### Backend
- **Node.js** 20+ (ES Modules)
- **TypeScript** 5.9+
- **Fastify** 5.6+ — веб-фреймворк
- **MongoDB** 6.20+ — база данных
- **@maxhub/max-bot-api** 0.2+ — SDK для работы с Max ботом

### Frontend
- **React** 18.3+
- **Vite** 7.1+ — сборщик и dev-сервер
- **TypeScript** 5.9+
- **@maxhub/max-ui** 0.1+ — UI компоненты для Max

### Инфраструктура
- **Docker** — контейнеризация
- **Docker Compose** — оркестрация контейнеров

---

## 📦 Требования

Перед запуском убедитесь, что у вас установлены:

- **Node.js** версии 20 или выше
- **npm** (обычно идет вместе с Node.js)
- **Docker** и **Docker Compose** (для запуска в контейнере)
- **MongoDB** (локально или удаленный доступ)

---

## 🚀 Установка и запуск

> **📢 Бот уже запущен на сервере!**  
> Вы можете попробовать его прямо сейчас: [https://max.ru/t176_hakaton_bot?startapp](https://max.ru/t176_hakaton_bot?startapp)

### Локальный запуск

> **⚠️ Внимание:** Если вы хотите запустить бота локально или через Docker, **обязательно используйте свой токен бота** в файле `.env`. Это необходимо, чтобы избежать конфликтов, так как один и тот же токен не может быть использован одновременно с двух устройств.

#### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url> vk-bot
cd vk-bot
```

#### Шаг 2: Установка зависимостей

```bash
# Установка зависимостей корневого проекта
npm install

# Установка зависимостей фронтенда
cd max-web
npm install
cd ..
```

#### Шаг 3: Настройка переменных окружения

Создайте файл `.env` в корне проекта со следующими переменными:

```bash
# Токен бота VK Max (обязательно укажите свой токен)
BOT_TOKEN=your_bot_token_here

# Настройки MongoDB (обязательные)
DB_USER=max
DB_PASSWORD='4&?dMn_ReA=Ir8R'
DB_HOST=51.250.2.8:27017
AUTH_SOURCE=admin
DB_CLIENT=max

# Настройки сети (обязательно)
API_LISTEN_URL=http://localhost:8788
VITE_API_URL=http://localhost:8788
WEB_URL=http://localhost:4173
WEB_APP_URL=http://localhost:4173
```

#### Шаг 4: Запуск приложения

Используйте скрипт `start.sh` для запуска всех компонентов:

```bash
chmod +x start.sh
./start.sh
```

Скрипт автоматически:
1. ✅ Загрузит переменные окружения из `.env`
2. ✅ Запустит API сервер на порту `8788` (http://localhost:8788)
3. ✅ Соберет фронтенд
4. ✅ Запустит preview сервер на порту `4173` (http://localhost:4173)
5. ✅ Запустит бота

#### Шаг 5: Проверка работы

После запуска вы должны увидеть в консоли:

```
✅ Подключено к MongoDB
✅ API успешно запущен: http://localhost:8788
✅ Бот успешно запущен
```

- **API**: доступен по адресу `http://localhost:8788`
- **Frontend**: доступен по адресу `http://localhost:4173`

#### ⚠️ Важное примечание о локальном запуске

При локальном запуске на новом боте **не будет мини-приложения**, следовательно:
- ❌ Не будет работать интеграция с VK Bridge
- ❌ Некоторые функции, зависящие от VK Bridge, будут недоступны
- ✅ Бот будет работать и обрабатывать события
- ✅ API будет полностью функционален
- ✅ Фронтенд можно открыть в браузере, но без интеграции с Max платформой

Для полной функциональности с мини-приложением и VK Bridge необходимо развернуть проект на сервере и настроить бота в панели разработчика VK Max.

### Запуск через Docker

> **⚠️ Внимание:** Обязательно используйте **свой токен бота** в файле `.env`, чтобы избежать конфликтов при одновременном запуске с сервера и локально.

#### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url> vk-bot
cd vk-bot
```

#### Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корне проекта со следующими переменными:

```bash
# Токен бота VK Max (обязательно укажите свой токен)
BOT_TOKEN=your_bot_token_here

# Настройки MongoDB (обязательные)
DB_USER=max
DB_PASSWORD='4&?dMn_ReA=Ir8R'
DB_HOST=51.250.2.8:27017
AUTH_SOURCE=admin
DB_CLIENT=max

# Настройки сети (обязательно)
API_LISTEN_URL=http://0.0.0.0:8788
VITE_API_URL=http://0.0.0.0:8788
WEB_URL=http://0.0.0.0:4173
WEB_APP_URL=http://0.0.0.0:4173
```

#### Шаг 3: Сборка и запуск

Соберите Docker образ и запустите контейнер:

```bash
docker compose up -d --build
```

Флаг `-d` запускает контейнер в фоновом режиме.

#### Шаг 4: Проверка работы

Проверьте статус контейнеров:

```bash
docker compose ps
```

Просмотр логов для отладки:

```bash
# Просмотр всех логов
docker compose logs -f

# Просмотр логов конкретного сервиса
docker compose logs -f vk-bot
```

#### Шаг 5: Остановка контейнеров

Когда нужно остановить контейнеры:

```bash
# Остановка с сохранением данных
docker compose stop

# Остановка и удаление контейнеров
docker compose down

# Остановка с удалением контейнеров и volumes
docker compose down -v
```

